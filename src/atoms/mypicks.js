import { atom } from 'recoil';

export const oneLineReviewState = atom({
  key: 'oneLineReviewState',
  default: [],
});

export const priceState = atom({
  key: 'priceState',
  default: '',
});

export const linkState = atom({
  key: 'linkState',
  default: [],
});

export const myPickDataState = atom({
  key: 'linkState',
  default: [],
});
